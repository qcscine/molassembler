set(CYCLIC_POLYGONS_VERSION 0.1.0)

find_package(molassembler COMPONENTS temple REQUIRED)

include(${CMAKE_CURRENT_LIST_DIR}/cyclic_polygonsTargets.cmake)

@PACKAGE_INIT@
set_and_check(CYCLIC_POLGONS_INCLUDE_DIR "@PACKAGE_INCLUDE_INSTALL_DIR@")

check_required_components(CYCLIC_POLYGONS)
