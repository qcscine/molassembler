find_package(molassembler COMPONENTS RingDecomposerLib gor1 boost_outcome cyclic_polygons temple chemical_symmetries stereopermutation json REQUIRED)
find_package(scine COMPONENTS Delib REQUIRED)
find_package(dlib REQUIRED)

set(MOLASSEMBLER_VERSION 0.1.0)

include(${CMAKE_CURRENT_LIST_DIR}/molassemblerTargets.cmake)

@PACKAGE_INIT@
set_and_check(MOLASSEMBLER_INCLUDE_DIR "@PACKAGE_INCLUDE_INSTALL_DIR@")

check_required_components(MOLASSEMBLER)
