find_package(molassembler COMPONENTS gor1 boost_outcome cyclic_polygons temple chemical_symmetries stereopermutation REQUIRED)
find_package(scine COMPONENTS Delib REQUIRED)

find_library(RDL_LIBRARY RingDecomposerLibStatic)
add_library(RingDecomposerLibStatic STATIC IMPORTED)
set_property(TARGET RingDecomposerLibStatic PROPERTY IMPORTED_LOCATION ${RDL_LIBRARY})

set(MOLASSEMBLER_VERSION 0.1.0)

include(${CMAKE_CURRENT_LIST_DIR}/molassemblerTargets.cmake)

@PACKAGE_INIT@
set_and_check(MOLASSEMBLER_INCLUDE_DIR "@PACKAGE_INCLUDE_INSTALL_DIR@")

check_required_components(MOLASSEMBLER)
