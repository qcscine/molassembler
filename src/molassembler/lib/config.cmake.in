include(CMakeFindDependencyMacro)

find_dependency(molassembler COMPONENTS RingDecomposerLib boost_outcome temple chemical_symmetries stereopermutation REQUIRED)
find_dependency(Scine COMPONENTS UtilsOS REQUIRED)
find_dependency(dlib REQUIRED)

if(NOT TARGET Boost::filesystem OR NOT TARGET Boost::system)
  unset(Boost_FOUND)
  find_dependency(Boost REQUIRED COMPONENTS filesystem system)
endif()

set(MOLASSEMBLER_VERSION 0.1.0)

include(${CMAKE_CURRENT_LIST_DIR}/molassemblerTargets.cmake)

@PACKAGE_INIT@
set_and_check(MOLASSEMBLER_INCLUDE_DIR "@PACKAGE_INCLUDE_INSTALL_DIR@")

check_required_components(MOLASSEMBLER)
